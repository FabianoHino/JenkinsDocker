properties([pipelineTriggers([githubPush()])])

pipeline {
    agent {
        docker {
            image 'fabianohino/teste-audiencia-api:latest'
            args '-i --entrypoint='
        }
    }
    stages {
        stage('Test') {
            steps {
                sh 'pytest'
            }
        }
    
        stage("Deployment") {
            steps {
                sshPublisher(publishers: [
                        sshPublisherDesc(configName: 'testeAudienciaHomologServer',
                        sshCredentials: [
                            encryptedPassphrase: '{encrypted_ssh_passphrase}', 
                            key: '', 
                            keyPath: '.ssh/id_rsa', 
                            username: 'admin'
                        ], 
                        transfers: [
                            sshTransfer(cleanRemote: false, 
                            excludes: '', 
                            execCommand: '''#!/bin/bash
                                docker cp /home/admin/testeAudiencia/app/main.py testeAudienciaHomolog:/app/main.py
                                docker container restart testeAudienciaHomolog
                            ''', 
                            execTimeout: 120000, 
                            flatten: false, 
                            makeEmptyDirs: true, 
                            noDefaultExcludes: false, 
                            patternSeparator: '[, ]+', 
                            remoteDirectory: '/home/admin/testeAudiencia/app/', 
                            remoteDirectorySDF: false, 
                            removePrefix: '', 
                            sourceFiles: 'app/*')
                        ], 
                    usePromotionTimestamp: false, 
                    useWorkspaceInPromotion: false, 
                    verbose: false) 
                ])
            }
        }
    }
}