pipeline {
  agent {
    docker {
        image 'fabianohino/teste-audiencia-api:latest'
        args '-i --entrypoint='
    }
  }
  stages {
    stage('Test') {
        steps {
            sh 'pytest'
        }
    }
  }
}